# Alle Variablen werden von der Root-.env.example übernommen.
# Lokale Entwicklung: Root-.env.local anlegen (alle drei Apps teilen sie via Symlink).
#
# Siehe: ../../.env.example
# Docs:  ../../docs/environment-setup.md
